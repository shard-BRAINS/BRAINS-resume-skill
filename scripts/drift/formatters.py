"""Drift formatters — human-readable presentation of drift score data.

Task 8 implements `format_headline_changes`. This stub keeps Task 7's
test green; it returns an empty list (which trivially satisfies the
"≤5 bullets" cap).
"""
from __future__ import annotations


def format_headline_changes(score: dict) -> list[str]:
    """Temporary stub — Task 8 will replace this with the real formatter."""
    return []
